{
  "name": "routeam-demos",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:comment": "npm --prefix comment-statistics-ui run build",
    "build:pages": "npm run build:comment && node scripts/build-pages.mjs",
    "deploy:pages": "npm run build:pages && gh-pages -d .pages-dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
